import { loadFont as loadHeadline } from "@remotion/google-fonts/Poppins";
import { loadFont as loadPrice } from "@remotion/google-fonts/PlayfairDisplay";
import { loadFont as loadCursive } from "@remotion/google-fonts/Caveat";

export const { fontFamily: pinterestHeadlineFont } = loadHeadline();
export const { fontFamily: pinterestPriceFont } = loadPrice();
export const { fontFamily: pinterestCursiveFont } = loadCursive();
